<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="geomex.xeus.sysmgr.service.LayerDescMapper">

	<select id="getList" parameterType="HashMap" resultType="LayerDescVo">
		SELECT mgr_seq, lyr_id, lyr_nm, grp_nm, reg_dat, json, is_use, idx

		FROM xeus.mt_lyr_desc

		<where>

			<if test="mgrSeq != null and mgrSeq != ''">
				AND mgr_seq = #{mgrSeq}::numeric
			</if>

			<if test="lyrId != null and lyrId != ''">
				AND lyr_id = #{lyrId}
			</if>

			<if test="lyrNm != null and lyrNm != ''">
				AND lyr_nm = #{lyrNm}
			</if>

			<if test="grpNm != null and grpNm != ''">
				AND grp_nm = #{grpNm}
			</if>

			<if test="regDat != null and regDat != ''">
				AND reg_dat = #{regDat}
			</if>

			<if test="json != null and json != ''">
				AND json = #{json}
			</if>

			<if test="isUse != null and isUse != ''">
				AND is_use = #{isUse}
			</if>

			<if test="idx != null and idx != ''">
				AND idx = #{idx}::numeric
			</if>

		</where>

		<trim prefix="ORDER BY">
			<if test="sortCol != null and sortCol != '' and sortTyp != null and sortTyp != ''">
				${sortCol} ${sortTyp}
			</if>
			<if test="sortCol == null and sortTyp == null">
				idx asc
			</if>
		</trim>

		<if test="limit != null and limit != '' and offset != null and offset != ''">
			LIMIT ${limit} OFFSET ${offset}
		</if>

	</select>


	<select id="getItem" parameterType="HashMap" resultType="LayerDescVo">
		SELECT mgr_seq, lyr_id, lyr_nm, grp_nm, reg_dat, json, is_use, idx

		FROM xeus.mt_lyr_desc

		<where>

			<if test="mgrSeq != null and mgrSeq != ''">
				AND mgr_seq = #{mgrSeq}::numeric
			</if>

			<if test="lyrId != null and lyrId != ''">
				AND lyr_id = #{lyrId}
			</if>

			<if test="lyrNm != null and lyrNm != ''">
				AND lyr_nm = #{lyrNm}
			</if>

			<if test="grpNm != null and grpNm != ''">
				AND grp_nm = #{grpNm}
			</if>

			<if test="regDat != null and regDat != ''">
				AND reg_dat = #{regDat}
			</if>

			<if test="json != null and json != ''">
				AND json = #{json}
			</if>

			<if test="isUse != null and isUse != ''">
				AND is_use = #{isUse}
			</if>

			<if test="idx != null and idx != ''">
				AND idx = #{idx}::numeric
			</if>

		</where>
	</select>


	<select id="getCount" parameterType="HashMap" resultType="int">
		SELECT count(*)

		FROM xeus.mt_lyr_desc

		<where>

			<if test="mgrSeq != null and mgrSeq != ''">
				AND mgr_seq = #{mgrSeq}::numeric
			</if>

			<if test="lyrId != null and lyrId != ''">
				AND lyr_id = #{lyrId}
			</if>

			<if test="lyrNm != null and lyrNm != ''">
				AND lyr_nm = #{lyrNm}
			</if>

			<if test="grpNm != null and grpNm != ''">
				AND grp_nm = #{grpNm}
			</if>

			<if test="regDat != null and regDat != ''">
				AND reg_dat = #{regDat}
			</if>

			<if test="json != null and json != ''">
				AND json = #{json}
			</if>

			<if test="isUse != null and isUse != ''">
				AND is_use = #{isUse}
			</if>

			<if test="idx != null and idx != ''">
				AND idx = #{idx}::numeric
			</if>

		</where>
	</select>


	<delete id="del" parameterType="HashMap">
		DELETE FROM xeus.mt_lyr_desc WHERE mgr_seq = #{mgrSeq}::numeric
	</delete>


	<insert id="add" parameterType="LayerDescVo">
		INSERT INTO xeus.mt_lyr_desc(
			lyr_id, lyr_nm, grp_nm, reg_dat, json, is_use
		) VALUES (
			#{lyrId}, #{lyrNm}, #{grpNm}, #{regDat}, #{json}, #{isUse}
		);
	</insert>


	<update id="edit" parameterType="LayerDescVo">
		UPDATE xeus.mt_lyr_desc SET
			mgr_seq = #{mgrSeq}::numeric

			<if test="lyrId != null and lyrId != ''">
				, lyr_id = #{lyrId}
			</if>

			<if test="lyrNm != null and lyrNm != ''">
				, lyr_nm = #{lyrNm}
			</if>

			<if test="grpNm != null and grpNm != ''">
				, grp_nm = #{grpNm}
			</if>

			<if test="regDat != null and regDat != ''">
				, reg_dat = #{regDat}
			</if>

			<if test="json != null and json != ''">
				, json = #{json}
			</if>

			<if test="isUse != null and isUse != ''">
				, is_use = #{isUse}
			</if>

			<if test="idx != null and idx != ''">
				, idx = #{idx}::numeric
			</if>

		WHERE mgr_seq = #{mgrSeq}::numeric

	</update>


</mapper>
