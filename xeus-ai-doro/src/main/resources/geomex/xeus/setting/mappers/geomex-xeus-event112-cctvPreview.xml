<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="geomex.xeus.event112.service.CctvPreviewMapper">

	<select id="getList" parameterType="HashMap" resultType="CctvPreviewVo">
		SELECT mgr_seq, req_user_id, req_resn, acci_num, cctv_mgr_no, req_dat, acpt_nm
				, cctv.cctv_nm
				, ST_X(ST_Centroid(cctv._geometry)) as tmx
				, ST_Y(ST_Centroid(cctv._geometry)) as tmy

		FROM xeus.mon_preview AS mon
		LEFT JOIN xeus.asset_cctv AS cctv ON mon.cctv_mgr_no = cctv.mgr_no

		<where>

			<if test="mgrSeq != null and mgrSeq != ''">
				AND mgr_seq = #{mgrSeq}::numeric
			</if>

			<if test="reqUserId != null and reqUserId != ''">
				AND req_user_id = #{reqUserId}
			</if>

			<if test="reqResn != null and reqResn != ''">
				AND req_resn = #{reqResn}
			</if>

			<if test="acciNum != null and acciNum != ''">
				AND acci_num = #{acciNum}
			</if>

			<if test="cctvMgrNo != null and cctvMgrNo != ''">
				AND mon.cctv_mgr_no = #{cctvMgrNo}
			</if>

			<if test="reqDat != null and reqDat != ''">
				AND req_dat like '%'||#{reqDat}||'%'
			</if>

			<if test="acptNm != null and acptNm != ''">
				AND acpt_nm = #{acptNm}
			</if>

			<if test="docSendYn != null and docSendYn != ''">
				AND doc_send_yn = #{docSendYn}
			</if>

			<if test="startDat!= null and startDat != ''">
                AND req_dat <![CDATA[ >= ]]> #{startDat}
            </if>

            <if test="endDat != null and endDat != ''">
                AND req_dat <![CDATA[ <= ]]> #{endDat}
            </if>

		</where>

		<trim prefix="ORDER BY">
			<if test="sortCol != null and sortCol != '' and sortTyp != null and sortTyp != ''">
				${sortCol} ${sortTyp}
			</if>
			<if test="sortCol == null and sortTyp == null">
				mgr_seq desc
			</if>
		</trim>

		<if test="limit != null and limit != '' and offset != null and offset != ''">
			LIMIT ${limit} OFFSET ${offset}
		</if>

	</select>


	<select id="getItem" parameterType="HashMap" resultType="CctvPreviewVo">
		SELECT mgr_seq, req_user_id, req_resn, acci_num, cctv_mgr_no, req_dat, acpt_nm

		FROM xeus.mon_preview

		<where>

			<if test="mgrSeq != null and mgrSeq != ''">
				AND mgr_seq = #{mgrSeq}::numeric
			</if>

			<if test="reqUserId != null and reqUserId != ''">
				AND req_user_id = #{reqUserId}
			</if>

			<if test="reqResn != null and reqResn != ''">
				AND req_resn = #{reqResn}
			</if>

			<if test="acciNum != null and acciNum != ''">
				AND acci_num = #{acciNum}
			</if>

			<if test="cctvMgrNo != null and cctvMgrNo != ''">
				AND cctv_mgr_no = #{cctvMgrNo}
			</if>

			<if test="reqDat != null and reqDat != ''">
				AND req_dat like '%'||#{reqDat}||'%'
			</if>

			<if test="acptNm != null and acptNm != ''">
				AND acpt_nm = #{acptNm}
			</if>

		</where>
	</select>


	<select id="getCount" parameterType="HashMap" resultType="int">
		SELECT count(*)

		FROM xeus.mon_preview

		<where>

			<if test="mgrSeq != null and mgrSeq != ''">
				AND mgr_seq = #{mgrSeq}::numeric
			</if>

			<if test="reqUserId != null and reqUserId != ''">
				AND req_user_id = #{reqUserId}
			</if>

			<if test="reqResn != null and reqResn != ''">
				AND req_resn = #{reqResn}
			</if>

			<if test="acciNum != null and acciNum != ''">
				AND acci_num = #{acciNum}
			</if>

			<if test="cctvMgrNo != null and cctvMgrNo != ''">
				AND cctv_mgr_no = #{cctvMgrNo}
			</if>

			<if test="reqDat != null and reqDat != ''">
				AND req_dat like '%'||#{reqDat}||'%'
			</if>

			<if test="acptNm != null and acptNm != ''">
				AND acpt_nm = #{acptNm}
			</if>

			<if test="docSendYn != null and docSendYn != ''">
				AND doc_send_yn = #{docSendYn}
			</if>

			<if test="startDat!= null and startDat != ''">
                AND req_dat <![CDATA[ >= ]]> #{startDat}
            </if>

            <if test="endDat != null and endDat != ''">
                AND req_dat <![CDATA[ <= ]]> #{endDat}
            </if>

		</where>
	</select>


	<delete id="del" parameterType="HashMap">
		DELETE FROM xeus.mon_preview WHERE mgr_seq = #{mgrSeq}
	</delete>


	<insert id="add" parameterType="CctvPreviewVo">
		INSERT INTO xeus.mon_preview(
			req_user_id, req_resn, acci_num, cctv_mgr_no, req_dat, acpt_nm
		) VALUES (
			#{reqUserId}, #{reqResn}, #{acciNum}, #{cctvMgrNo}, #{reqDat}, #{acptNm}
		);
	</insert>


	<update id="edit" parameterType="CctvPreviewVo">
		UPDATE xeus.mon_preview SET
			mgr_seq = #{mgrSeq}::numeric

			<if test="reqUserId != null and reqUserId != ''">
				, req_user_id = #{reqUserId}
			</if>

			<if test="reqResn != null and reqResn != ''">
				, req_resn = #{reqResn}
			</if>

			<if test="acciNum != null and acciNum != ''">
				, acci_num = #{acciNum}
			</if>

			<if test="cctvMgrNo != null and cctvMgrNo != ''">
				, cctv_mgr_no = #{cctvMgrNo}
			</if>

			<if test="reqDat != null and reqDat != ''">
				, req_dat = #{reqDat}
			</if>

			<if test="acptNm != null and acptNm != ''">
				, acpt_nm = #{acptNm}
			</if>

		WHERE mgr_seq = #{mgrSeq}::numeric

	</update>

	<update id="editDocSendYn" parameterType="HashMap">

		<if test="mgrSeqList != null">
			UPDATE xeus.mon_preview SET
			doc_send_yn = 'Y'
			WHERE mgr_seq IN

			<foreach item="item" collection="mgrSeqList" index="index" open="(" separator="," close=")">
				#{item}::integer
			</foreach>
		</if>

	</update>

</mapper>
